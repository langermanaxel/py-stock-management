# Procfile para despliegue en Railway/Render
# Define los procesos que debe ejecutar la plataforma

# Proceso web principal (obligatorio)
web: python run.py

# Proceso de worker para tareas en background (opcional)
# worker: python -m celery worker --loglevel=info

# Proceso de beat para tareas programadas (opcional)
# beat: python -m celery beat --loglevel=info

# Proceso de migración de base de datos (opcional)
# migrate: python -m flask db upgrade

# Proceso de inicialización de datos (opcional)
# init: python init_sample_data.py
